meta {
  name: public
  seq: 2
}

auth {
  mode: none
}

docs {
  Unauthenticated endpoints -- everything the marketing site calls.

  Read endpoints accept `?lang=en|es` (falling back to the `Accept-Language`
  header, then English) on anything with translated copy: packages, blogs,
  testimonials, teacher bios.
}
